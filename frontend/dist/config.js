/**
 * Runtime configuration. Keep apiBase empty for the self-contained GitHub Pages demo.
 * Example for a separately deployed API: apiBase: 'https://api.example.com/api/v1'
 */
window.LUNARIA_CONFIG = Object.freeze({
  apiBase: '',
  storeMode: 'demo',
  currency: 'EUR',
  deliveryFeeCents: 800,
});
